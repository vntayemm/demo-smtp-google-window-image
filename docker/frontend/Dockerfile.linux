# Linux fallback frontend
FROM node:20-alpine
WORKDIR /app
ENV NODE_ENV=production
ENV PORT=8080
COPY frontend/index.html ./index.html
COPY docker/frontend/serve.js ./serve.js
EXPOSE 8080
CMD ["node", "serve.js"]
